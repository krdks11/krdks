<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Submission - Admin</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <div class="submission-detail">
            <div class="detail-header">
                <a href="/admin/submissions" class="back-btn">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
                <h1><i class="fas fa-file-alt"></i> Submission Details</h1>
            </div>

            <div class="detail-card">
                <div class="detail-section personal-info">
                    <h2><i class="fas fa-user"></i> Personal Information</h2>
                    <div class="detail-row">
                        <label><i class="fas fa-user-circle"></i> Name:</label>
                        <p><%= submission.full_name %></p>
                    </div>
                    <div class="detail-row">
                        <label><i class="fas fa-envelope"></i> Email:</label>
                        <p><%= submission.email %></p>
                    </div>
                    <div class="detail-row">
                        <label><i class="fas fa-phone"></i> Mobile:</label>
                        <p><%= submission.mobile_number %></p>
                    </div>
                </div>

                <div class="detail-section project-info">
                    <h2><i class="fas fa-project-diagram"></i> Project Information</h2>
                    <div class="detail-row">
                        <label><i class="fas fa-tasks"></i> Project Type:</label>
                        <p><%= submission.project_type %></p>
                    </div>
                    <div class="detail-row">
                        <label><i class="fas fa-money-bill-wave"></i> Budget Range:</label>
                        <p><%= submission.budget_range %></p>
                    </div>
                    <div class="detail-row">
                        <label><i class="fas fa-clock"></i> Timeline:</label>
                        <p><%= submission.timeline %></p>
                    </div>
                </div>

                <div class="detail-section status-section">
                    <h2><i class="fas fa-flag"></i> Status Information</h2>
                    <div class="detail-row">
                        <label><i class="fas fa-calendar-alt"></i> Submitted On:</label>
                        <p><%= new Date(submission.submission_date).toLocaleString() %></p>
                    </div>
                    <div class="detail-row status-update-row">
                        <label><i class="fas fa-tasks"></i> Current Status:</label>
                        <select id="statusUpdate" data-id="<%= submission.id %>" class="status-select">
                            <option value="new" <%= submission.status === 'new' ? 'selected' : '' %>>New</option>
                            <option value="in_progress" <%= submission.status === 'in_progress' ? 'selected' : '' %>>In Progress</option>
                            <option value="completed" <%= submission.status === 'completed' ? 'selected' : '' %>>Completed</option>
                            <option value="archived" <%= submission.status === 'archived' ? 'selected' : '' %>>Archived</option>
                        </select>
                    </div>
                </div>

                <div class="detail-section project-details-section">
                    <h2><i class="fas fa-file-alt"></i> Project Details</h2>
                    <div class="detail-row full">
                        <p class="project-details"><%= submission.project_details %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/admin.js"></script>
</body>
</html> 